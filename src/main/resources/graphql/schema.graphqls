type Query {
    customers: [Customer]
}

type Customer {
    customerId: ID!
    name: String!
    orders: [Order]
}

type Order {
    orderId: ID!
    customerId: Int
    products: [Product]
}

type Product {
    productId: ID!
    orderId: Int
}